# 5yberBind — Premium Editing Assets

Ready-to-run React + Vite + Tailwind starter for an editing-assets storefront.

## Quick start
1. Ensure Node.js (v16+) is installed.
2. Copy files into a new project folder or clone this repo.
3. Install & run:
   ```bash
   npm install
   npm run dev